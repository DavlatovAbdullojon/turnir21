# Турнирный Telegram-бот (Школа 21) — aiogram 3 + SQLite

Бот для турнира по настольному теннису:
- формат B1 (олимпийка / single elimination)
- 16 участников (если меньше — старт на 8/4/2)
- динамическая очередь матчей (без расписания)
- 2 стола
- вызов на матч: 5 минут
- 1 задержка (⏳), второй раз = техпоражение
- подтверждение результата / спор → admin_review

## 1) Установка

### Windows (PowerShell)
```powershell
cd "C:\Users\...\Desktop\Турнир Школа 21"
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

## 2) Настройка .env

Создай файл `.env` в корне проекта:

```env
BOT_TOKEN=ВАШ_ТОКЕН
ADMIN_IDS=123456789,987654321
DB_PATH=tournament.db
```

> Совет: если у тебя часто меняется рабочая папка (VS Code запускает из другого места), можно указать абсолютный путь:
> `DB_PATH=C:\Users\Davlatov Abdullojon\Desktop\Турнир Школа 21\tournament.db`

## 3) Запуск
```powershell
python main.py
```

## 4) Проверка бота двумя аккаунтами (2 участника)

### Вариант A — два Telegram аккаунта на телефоне
1. Открой Telegram → **Настройки → Добавить аккаунт**
2. Зайди во второй аккаунт (другой номер)
3. В обоих аккаунтах открой своего бота
4. В аккаунте 1: `/start` → 📝 Регистрация → отправь имя
5. В аккаунте 2: `/start` → 📝 Регистрация → отправь имя
6. Админ (твой основной аккаунт из ADMIN_IDS):
   - `/admin` → 🟢 Открыть check-in
7. Оба аккаунта нажимают: ✅ Я на месте
8. Админ: `/admin` → 🚀 Старт турнира
9. Бот вызовет матч на стол (1 или 2):
   - оба нажимают ✅ Я рядом → матч стартует
10. После игры один нажимает 🏁 Сообщить результат → выбирает счёт
11. Второй нажимает ✅ Подтвердить

### Вариант B — Telegram Desktop + Telegram на телефоне
- Аккаунт 1 в Telegram Desktop
- Аккаунт 2 в Telegram на телефоне

## 5) Частые проблемы

### «Мой статус» пишет что не зарегистрирован
- Убедись, что ты **после кнопки Регистрация отправил имя** (сообщением).
- Убедись, что запущен **только один экземпляр** бота.
- Проверь DB_PATH: если база создаётся в другой папке, регистрация и статус будут смотреть в разные файлы.

## 6) Что дальше можно улучшить
- King of the Table режим (после финала) — легко добавить отдельным модулем
- логирование событий в отдельную таблицу
- красивый вывод сетки в виде таблицы
